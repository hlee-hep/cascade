# Cascade

Modular HEP analysis framework with support for ROOT, RDataFrame, DAG-based execution, and snapshot tracking.

## Structure
- `src/` — Core framework
- `modules/` — Individual analysis modules
- `python/` — Python wrapper
- `main` - pybind11

## Build
Built using `scons`. See `SConstruct`.
